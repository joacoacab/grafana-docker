# Grafana config — Phase 1
# Admin credentials are injected via GF_SECURITY_ADMIN_USER / GF_SECURITY_ADMIN_PASSWORD env vars

[analytics]
reporting_enabled = false
check_for_updates = false
enable_feedback_links = false

[users]
allow_sign_up = false

[auth.anonymous]
enabled = false
